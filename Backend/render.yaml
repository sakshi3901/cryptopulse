# Exported from Render on 2026-07-27T16:24:28Z
version: "1"
projects:
- name: My project
  environments:
  - name: Production
    services:
    - type: web
      name: Cyptopulse
      runtime: docker
      repo: https://github.com/sakshi3901/cryptopulse
      plan: free
      envVars:
      - key: google_client_id
        sync: false
      region: oregon
      buildCommand: pip install -r requirements.txt
      startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
      autoDeployTrigger: commit
      rootDir: Backend
